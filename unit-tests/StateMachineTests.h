#ifndef _STATE_MACHINE_TESTS_H
#define _STATE_MACHINE_TESTS_H

// Run all StateMachine unit tests. Each test uses ASSERT_TRUE — any failure
// aborts via FaultHandler. Prints "All StateMachine tests passed." on success.
void RunStateMachineTests();

#endif // _STATE_MACHINE_TESTS_H
